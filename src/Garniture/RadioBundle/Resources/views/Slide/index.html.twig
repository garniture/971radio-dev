{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Slide list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Slug</th>
                <th>Titre</th>
                <th>Texte</th>
                <th>Links</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('slide_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.name }}</td>
                <td>{{ entity.titre }}</td>
                <td>{{ entity.texte }}</td>
                <td>{{ entity.links }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('slide_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('slide_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('slide_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
